package String;

public class string {
    public static void main(String[] args) {
        String a= "abc";
        System.out.println(a.compareTo("bcd"));
        /*
        .charAt(index) 해당 index 의 값 반환 (char)
        .compareTo(String) 해당 문자열과 비교후 차이값 반환 (int)
        .concat(String) 문자열 끝에 다른 문자열 추가
        .contains(Char) 문자열에 해당 문자가 있는지 반환 (boolean)
        .equals(String) 두 문자열에 차이가 있는지 반환 (boolean)
        .indexOf(Char) 문자열에 해당 문자에 첫 문자의 index 값 반환
        .isEmpty() 문자열이 비어있는지 확인
        .lastIndexOf(Char)  문자열에 해당 문자에 마지막 문자의 index 값 반환
        .length() 문자열 길이값 반환
        .replace(Char, Char) 문자열에 해당 char 값을 다른 char 값으로 교체
        .replaceAll(String, String) 문자열에 해당 문자열 값을 다른 문자열 값으로 교체
        .split() 문자열을 하위 문자열로 분할
        .toLowerCase() 모든 문자를 소문자로 변환
        .toUpperCase() 모든 문자를 대문자로 변환
        .toString() 해당 객체값 반환
        .trim() 양끝 공백값 제거
         */
    }
}
